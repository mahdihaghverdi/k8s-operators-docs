[project]
name = "k8s-operators-docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mahdihaghverdi", email = "mahdihaghverdiliewpl@gmail.com" }
]
requires-python = ">=3.12.3"
dependencies = []

[project.scripts]
k8s-operators-docs = "k8s_operators_docs:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.53",
]
